# 변수 재사용(vars_file, include_vars)

2. `~/lab/ex08` 디렉터리를 만들고  `site_settings.yml`(`banner_text`, `http_port`)과 `queue_vars.yml`(`queue_name`, `queue_user`) 두 변수 파일을 작성하세요.
3. `vars_files` 키워드로 `site_settings.yml`을 가져와 값을 출력하는 플레이북을 `varsfiles.yml`에 작성해 실행하세요.
4. `include_vars` 모듈로 `site_settings.yml`을 축약형으로, `queue_vars.yml`은 `name: cfg`를 지정해 각각 가져오는 작업을 `includevars.yml`에 작성해 `cfg.queue_name` 형태로 참조해 보세요.
5. 같은 파일에 `include_vars`를 `when` 조건 뒤에 붙인 작업을 추가해, 조건이 거짓일 때는 파일을 읽지 않는다는 것을 확인하세요.
6. `vars_files`와 `include_vars` 중 "조건부로만 읽어야 하는 경우"에 어느 쪽을 써야 하는지 결론을 코드 주석으로 남기세요.

---

# 변수 재사용(group_vars, host_vars)

1.  `group_vars`, `host_vars` 디렉터리를 구성해 같은 변수 이름을 여러 곳에 정의하세요.
2. `site.yml`에서 이 변수를 출력하는 작업을 작성해 실행하고, 플레이북 경로의 값이 인벤토리 경로의 값을 이기는지 확인하세요.
3. `host_vars`가 있는 `ansible-node1`과, 없는 `ansible-node2`에서 값이 어떻게 다른지 비교하세요.
4. `webservers` 그룹명 디렉터리 안에 파일 두 개(`01-base.yml`, `99-override.yml`)를 만들어 같은 변수를 정의하고, 알파벳순으로 나중 파일이 이기는지 확인하세요.
5. `group_vars/all`과 개별 그룹 파일에 같은 변수를 정의해 개별 그룹 파일이 우선하는지 확인하세요.

---

# 작업 재사용

1. 공통 작업 파일(`helpers.yaml`, `stepA`/`stepB` 두 작업)을 만들고, `include_tasks`로 가져오는 플레이북(`runtime_include.yml`)과 `import_tasks`로 가져오는 플레이북(`compile_time_import.yml`)을 각각 작성하세요.
2. 두 플레이북에 `--list-tasks`를 실행해 내부 작업이 보이는지 여부를 비교하세요.
3. `salute.yaml`(`item` 인사)을 `include_tasks` + `loop`로 반복 실행하는 `dynamic_loop.yml`을 작성해 실행해 보고, 같은 것을 `import_tasks` + `loop`로 바꾼 `static_loop.yml`을 실행해 오류가 나는지 확인하세요.
4. 내부 작업에 태그를 붙인 파일(`sectioned.yaml`)을 각각 `include_tasks`(`tag_dynamic.yml`) / `import_tasks`(`tag_static.yml`)로 가져와 `--tags`로 내부 작업만 선택할 수 있는지 비교하세요.
5. 4번의 두 플레이북에 각각 `--start-at-task`로 정적/동적 방식에서 내부 작업까지 시작 지점으로 지정할 수 있는지 비교하세요.
