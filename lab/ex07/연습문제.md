# 오류 처리

1. `~/lab/ex07` 디렉터리를 만들고, 실제로 존재하지 않는 호스트(예: `ansible_host=192.168.56.199`)를 `unreachable_hosts` 그룹으로 담은 별도 인벤토리 파일 `unreachable.ini`를 작성하세요.
2. `unreachable.ini`로  `unreachable_test.yml`을 실행해 `UNREACHABLE`이 되는 것을 확인한 뒤, `ignore_unreachable: true`를 추가해 후속 작업까지 실행되는지 비교하세요.
3. `ansible-node1`에서 존재하지 않는 경로를 조회하는 명령을 `ignore_errors: true`로 무시하는 작업과, `ignore_unreachable`로는 이 실패를 막을 수 없다는 것을 확인하는 작업을 `ignore_errors_test.yml`에 작성하세요.
4. `echo` 명령의 출력에 특정 문자열이 있는지로 `failed_when`을 재정의하는 작업과, 반환 코드가 0이어도 실패로 간주하는 작업을 `failed_when_test.yml`에 작성하세요.
5. `changed_when: false`를 조회 전용 명령에 붙이고, 조건부로 `changed`를 보고하는 작업을 `changed_when_test.yml`에 작성해 비교하세요.

---

# 전략과 롤링 업데이트

1. `ansible-node1`만 4초 지연시키는 작업과 그다음 작업으로 구성된 플레이를 `linear`(기본) 전략과 `strategy: free` 전략 두 가지로 `strategy_compare.yml`에 작성해 비교하세요.
2. `serial_compare.yml`에 `nodes` 그룹을 대상으로 하는 플레이를 작성해, `serial` 없이 실행한 결과와 `serial: "50%"`를 추가했을 때 `PLAY` 블록이 몇 번 나타나는지 비교하세요.
3. `throttle: 1`을 특정 작업에 지정한 `throttle_test.yml`을 작성해, 두 호스트가 동시에 시작하지 않고 순차적으로 시작하는지 시작 시각을 찍어 확인하세요.
4. `forks` 값을 `ansible.cfg` 또는 `-f` 옵션으로 바꿔서 `strategy_compare.yml`을 실행해 보고 `ansible-config dump`로 반영되었는지 확인하세요.
5. `serial: [1, "50%"]`처럼 목록으로 배치 크기를 늘려 가는 카나리 배포 형태의 플레이를 `canary.yml`에 작성해 보세요(실행은 선택).

---

# 비동기

1. `ansible-node1`에 `-B 900 -P 0` 옵션으로 `sleep 40` ad-hoc 명령을 비동기 실행하고, 반환된 `ansible_job_id`를 기록하세요.
2. `async_status` 모듈로 그 작업 ID의 상태를 조회해, `finished` 값이 `false`에서 `true`로 바뀌는 것을 확인하세요.
3. `async_practice.yml`에 `async: 60`, `poll: 10`으로 실행되는 `sleep 20` 작업을 작성해, 폴링 로그(`ASYNC POLL`)가 출력되는지 확인하세요.
4. 같은 파일에 `poll: 0`으로 fire-and-forget 작업을 추가하고, 그 사이에 다른 작업이 먼저 끝나는지 확인하세요.
5. `until`, `retries`, `delay`와 `async_status`를 조합해 4번의 비동기 작업이 끝날 때까지 기다렸다가 최종 결과(`rc`, `delta`)를 출력하는 작업을 추가해 `async_practice.yml`을 실행하세요.

---

# 태그

1. `backup`, `database` 두 태그가 붙은 작업 하나, `verification` 태그가 붙은 작업 하나, `always`·`never` 특수 태그가 붙은 작업 각각 하나, 태그 없는 작업 하나로 구성된 백업 시나리오 플레이북을 `backup_tags.yml`로 작성하세요.
2. `--tags "verification,database"`로 실행해 어떤 작업이 실행되는지, `always` 태그 작업이 함께 실행되는지 확인하세요.
3. `--skip-tags database`로 실행해 결과를 비교하세요.
4. `--tags untagged`와 `--tags tagged`를 각각 실행해 차이를 확인하세요.
5. `--tags never`를 명시적으로 지정했을 때만 `never` 작업이 실행되는지 확인하고, `--list-tags` / `--list-tasks`로 전체 태그와 작업 목록을 미리 확인하세요.

---

# 작업 시작 및 단계

1. 앞 실습에서 만든 태그 플레이북(`backup_tags.yml`)을 `--start-at-task="Write log entry"`로 실행해, 앞의 작업들이 건너뛰어지는지 확인하세요(`always` 태그 작업도 건너뛰어지는 점에 주목하세요).
2. 같은 플레이북을 `--step` 옵션으로 실행해, 각 작업마다 `y`/`n`/`c`로 실행 여부를 선택해 보세요.
3. `--list-tasks`로 확인했던 작업 이름 중 하나를 `--start-at-task`에 지정해 정확히 그 작업부터 시작하는지 확인하세요.
4. 이름이 없는 작업에는 `--start-at-task`를 쓸 수 없다는 것을 직접 시도해 확인하세요(또는 이유를 정리해 보세요).
