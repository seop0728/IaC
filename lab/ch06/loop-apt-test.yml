---
- hosts: localhost
  vars_files:
    - vars.yml

  tasks:
    - name: list
      debug:
        msg: "{{ package_list }}"
    - name: loop
      debug:
        msg: "{{ item }}"
      loop: "{{ package_list }}"
